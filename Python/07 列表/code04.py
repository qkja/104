"""
元组的操作
"""

# 功能上他们是类似的
# 创建元组
# a = ()
# b = tuple()
# print(type(a))
# print(type(b))

# a = (1,2,3,4,5)
# print(type(a))
# print(a)
# 和列表类似,元组里面的元素也是可以任意类型
# 通过下标访问元组的元素,也是从0开始的
# 可以使用切片来获取部分元素
# 元组可以使用for等方式类遍历元素
# 可以使用in,index来操作

# 上面所有的操作,都是只读的, 凡是需要修改的,这里都不行

# def getPoint():
#     x = 10
#     y = 20
#     return x, y
# x, y = getPoint()
# 返回值这里本质是元组
# print(type(getPoint()))

# 为何出现元组,这个只读不修改的操作


