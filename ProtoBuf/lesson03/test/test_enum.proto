// 测试枚举类型
syntax = "proto3";

// 枚举可以在消息体内
enum PhoneType
{
  MP = 0; // 0 值必须存在,且作为 第一个 禁止负值,在32位整数位
  TEL = 1;  
}

// 两个枚举,存在相同 的常量名称出现问题
// 引用其他的文件也是如此
// 我们这里可以使用packsge, 以至于形成类域
// enum PhoneTypeCopy
// {
//   // MP = 0; 
//   MP_C = 0; 
// }
