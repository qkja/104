#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<Windows.h>
#include<stdlib.h>

int main()
{




	/*char *p = (char*)malloc(sizeof(char)* 10);
	printf("%p\n", p);
	printf("%p\n", &p);
	free(p);
	printf("%p\n", p);
	p = NULL;*/
	//int i = 0;
	//while (1 < 10000)
	//{
    //        char *p = (char*)malloc(sizeof(char)* 10);
	//	 i++;
	//}
	
	system("pause");
	return 0;
}